<% @car.reviews.each do |review|%>
<p>
  <strong>Comment:</strong>
  <%= review.description%>
</p>

<div>
  <% if Like.where(user: current_user.id).where(likeable_id: review.id).present? %>
  <p>👍 
  <%= link_to "👎", likes_path(review_id: review.id, like_id:@like),:method => :delete %> </p>
  <% else %>
  <p><%= link_to "👍", likes_path(review_id: review.id), :method => :post %>
   👎</p>
  <% end %>
  <em><%= review.likes.count%></em><br>  
</div>

<% if current_user.id == review.user_id %>
<p>
  <%= link_to "Remove Review", [review.car, review],
    method: :delete, data: {confirm: "Are you sure? "} %> 
</p>
<% end %>
<% end %>
